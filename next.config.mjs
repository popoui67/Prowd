/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.unsplash.com", "lh3.googleusercontent.com",'next-images-uploader', "files.edgestore.dev"],
      }
    };

export default nextConfig;
